# Gem Station Alpha

A tiny planet-hopping gem station minigame for Jojo.

## Scripts

- `npm run dev` starts Vite locally.
- `npm run build` type-checks and builds the site.
- `npm run preview` previews the production build.

The site is configured for GitHub Pages at `/gem-station-alpha/`.
